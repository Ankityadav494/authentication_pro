<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body class="bg-gray-100 min-h-screen flex flex-col items-center justify-center">
    <h1 class="text-2xl font-bold text-blue-600 mb-4">Hey, <%= user.name %>!</h1>
    <a href="/logout" class="text-red-500 hover:underline mb-6">Log Out</a>
    <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md">
        <form action="/post" method="post" class="space-y-4">
            <label for="aboutpost" class="block text-gray-700 font-medium mb-2">What's on your mind?</label>
            <textarea 
                id="aboutpost" 
                name="content" 
                rows="4" 
                placeholder="Share your thoughts..." 
                class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-400 resize-none"
                required
            ></textarea>
            <input 
                type="submit" 
                value="Create Post" 
                class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 rounded cursor-pointer transition"
            >
        </form>
        
    </div>
    <!-- posts -->
     
     <% user.posts.reverse().forEach(function(post){ %>
    
        <div>
            <div class="post bg-gray-50 rounded-lg shadow-sm p-4 mt-6">
                <div class="flex items-center mb-2">
                    
                    <h4 class="font-semibold text-gray-800">@<%=user.username%></h4>
                </div>
                <p class="text-gray-700 mb-4"><%=post.content%></p>
                <div class="flex space-x-4">
                    <a href="" class="text-blue-500 hover:text-blue-700 font-medium transition">Like</a>
                    <a href="" class="text-green-500 hover:text-green-700 font-medium transition">Edit</a>
                </div>
            </div>
        </div>
        <% }); %>
</body>
</html>